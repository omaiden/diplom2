from django.contrib import admin
from django.urls import path
from . import views


urlpatterns = [
    # path('missing', views.post_missing, name='post_missing'),
    # # path('victim', views.victim),
    # path('thanks/', views.thanks, name='thanks'),
    #path('posts/', views.PostList.as_view(template_name='posts.html'), name='post'),
    # path('image/', views.image),
    # path('test/', views.test),
    # path('<int:pk>/', views.mypost, name='post')
]


